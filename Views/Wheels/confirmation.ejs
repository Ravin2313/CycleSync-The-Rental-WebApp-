<% layout("/layouts/boilerplate") %>
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Confirmation</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/confirmation.css" />
  </head>
  <body class="bg-dark text-white">
    <div class="container my-5">
      <div class="card shadow-lg">
        <div class="card-header bg-primary text-white text-center">
          <h2>Confirm Your Rental</h2>
        </div>
        <div class="card-body">
          <!-- Wheel Details -->
          <h4 class="text-center mb-4">Wheel Details</h4>
          <div class="row">
            <div class="col-md-6 d-flex justify-content-center">
              <img
                src="<%= wheel.image %>"
                alt="<%= wheel.modelName %>"
                class="confirmation-image rounded"
              />
            </div>
            <div class="col-md-6">
              <ul class="list-group">
                <li class="list-group-item">
                  <strong>Model Name:</strong> <%= wheel.modelName %>
                </li>
                <li class="list-group-item">
                  <strong>Type:</strong> <%= wheel.type %>
                </li>
                <li class="list-group-item">
                  <strong>Rent Price (per day):</strong> ₹<%= wheel.rentPrice %>
                </li>
                <li class="list-group-item">
                  <strong>Condition:</strong> <%= wheel.condition %>
                </li>
              </ul>
            </div>
          </div>

          <hr />

          <!-- User Details -->
          <!-- <h4 class="text-center mb-4">User Details</h4>
          <ul class="list-group">
            <li class="list-group-item">
              <strong>Name:</strong> <%= userDetails.name %>
            </li>
            <li class="list-group-item">
              <strong>Email:</strong> <%= userDetails.email %>
            </li>
            <li class="list-group-item">
              <strong>Contact Number:</strong> <%= userDetails.contactNumber %>
            </li>
            <li class="list-group-item">
              <strong>Rental Days:</strong> <%= userDetails.rentalDays %>
            </li>
            <li class="list-group-item">
              <strong>Pickup Date:</strong> <%= userDetails.pickupDate %>
            </li>
            <li class="list-group-item">
              <strong>Payment Method:</strong> <%= userDetails.paymentMethod %>
            </li>
          </ul> -->

          <hr />

          <!-- Confirm and Cancel Buttons -->
          <div class="text-center">
            <!-- Pay Now Button -->
            <form
              action="/wheels/<%= wheel._id %>/payment"
              method="GET"
              class="d-inline"
            >
              <button type="submit" class="btn btn-success btn-lg">
                Pay Now
              </button>
            </form>
            <!-- Cancel Button -->
            <a href="/wheels" class="btn btn-danger btn-lg">Cancel</a>
          </div>
        </div>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-w76AWWa8f5yM2zE01B4CF77RLsIWbMWG5lZG8R9qofKOz2MkFV9Q8SR3r0K3sm4"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
